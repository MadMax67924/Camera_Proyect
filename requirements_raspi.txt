# ============================================================================
# DEPENDENCIAS OPTIMIZADAS PARA RASPBERRY PI
# ============================================================================
# Este archivo está optimizado para Raspberry Pi con conexión lenta
# Evita paquetes muy pesados y usa versiones precompiladas (wheels)

flask>=2.0.0
opencv-python>=4.5.0
numpy>=1.19.0
Pillow>=8.0.0
scikit-learn>=0.24.0

# NO incluye scipy directamente (se instalará como dependencia de scikit-learn)
# NO incluye mediapipe (usa Haar Cascade en su lugar - más rápido en RPi)
# NO incluye bleak (PySerial es más ligero para Arduino)

pyserial>=3.5
